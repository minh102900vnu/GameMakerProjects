// hướng bay
dir = random(360);
spd = random_range(0.2, 0.5);

// đổi hướng
change_dir_timer = irandom_range(30, 90);

// bay lượn
t = random(1000);
offset = random(1000);

// nhấp nháy
blink_speed = random_range(0.3, 0.6);
blink_offset = random(1000);